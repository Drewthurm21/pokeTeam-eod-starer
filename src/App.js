import React from "react";

/*
  Routing practice:
    react-router-dom
    Router, Routes, Route

  Use react-router-dom to handle routing for our
    - create a route for the navbar
      *it should show on every page except the splash page
    - create a route for the splash page
      *it should show on the splash page ('/') only  
    - create a route for the homepage
      *it should show on the homepage ('/home') only

*/

function App() {

  return <h1>Pokemon Team Builder!</h1>
}

export default App;
